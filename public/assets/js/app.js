const btn=document.getElementById('menuBtn'),nav=document.getElementById('nav');btn?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('#nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.getElementById('form')?.addEventListener('submit',e=>{e.preventDefault();document.getElementById('msg').textContent='Pesan tersimpan sebagai simulasi. Hubungkan form ke backend/email service saat deployment.';e.target.reset();});